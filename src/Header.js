const Header = props => {
    return (
        <div className="header">
            <div className="title">Memory Game</div>
            <div className="instructions">
                Select an different image every round, avoiding images you've already clicked.
            </div>
        </div>
    )
}

export default Header